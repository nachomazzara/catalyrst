[package]
name = "catalyrst-testgate"
version = "0.20.0"
edition = "2021"
description = "Arms env-gated integration suites: a missing dependency fails the test naming the variable, and skipping requires the explicit ALLOW_SKIPPED_INTEGRATION opt-out"

[dependencies]
