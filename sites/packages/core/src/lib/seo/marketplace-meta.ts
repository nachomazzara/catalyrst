export function marketplaceMeta(section?: string) {
  return [
    {
      title: section
        ? `${section} \u{2014} Marketplace | Decentraland`
        : "Marketplace | Decentraland",
    },
  ];
}
